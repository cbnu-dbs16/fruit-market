import styled from 'styled-components'

const StyledCart = styled.div`
    .content{
        // display: flex;
        width: 1050px;
        margin: 0 auto;
        padding-top: 20px;
    }
`

export default StyledCart;